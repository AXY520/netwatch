# Netwatch 项目全局注意事项

## 公网请求与探测

本项目必须尽可能减少短时间内向公网发起的请求。任何新增或修改的公网探测、连通性检查、延迟检测、域名解析及后台刷新逻辑，都必须遵守以下约束：

- 优先复用已有结果，并为结果设置合理缓存；后台自动探测的默认间隔不得短于 5 分钟。
- 用户打开页面或窗口时，优先使用仍然新鲜的缓存（通常可采用 1～2 分钟 TTL），不得无条件重新探测。
- 用户手动触发的探测也必须有短缓存、防抖或限流，不能通过连续点击绕过请求限制。
- 同一探测的并发调用必须合并，避免多个请求同时执行相同的公网访问。
- 每项探测必须明确设置总请求预算、并发上限和短超时；成功后立即停止，不得为了覆盖率继续请求其他目标。
- 公网目标采用“一个主目标，失败后按需尝试最多一个备用目标”的方式；备用目标应尽量来自不同服务商，避免连续请求同一服务商的重复目标。
- 禁止一次并发请求大量公网目标。只有在区分故障层级确有必要时，才能增加额外请求，并应复用已经获得的 DNS、连接或响应结果。
- 自动化测试默认禁止访问真实公网，必须使用本地测试服务器、依赖注入或 mock。
- 开发、调试和生产诊断同样遵守上述约束；不得以临时排查为由进行高频、无上限的公网请求。
